import { styled } from "@mui/system";

export const Container = styled('div')(({theme}) => `
    background: #1e1e46;
    height: 100%;
    position: fixed;
    width: 100%;
    overflow-y: scroll;
    color: white;
`,)

